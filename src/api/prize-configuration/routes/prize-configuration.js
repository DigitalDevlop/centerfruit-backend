'use strict';

/**
 * prize-configuration router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::prize-configuration.prize-configuration');
